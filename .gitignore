# secrets & local machine config — never commit
.env
.mcp.json

# python
.venv/
__pycache__/
*.pyc
*.egg-info/

# data dump (derived clinical data + PII, and an 84MB tarball) — keep local only.
# but DO commit small synthetic config like the price/inventory CSV.
data/patient_graph/
data/*.tar.gz

# generated forecast output (regenerable; may contain pet names)
out/

# os / editor
.DS_Store
.idea/
.vscode/
